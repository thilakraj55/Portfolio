# TODO - Website build & debug

- [x] Step 1: Run the Flask app and reproduce the current error (if any).
- [x] Step 2: Inspect Flask template/static folder configuration and make it work while keeping the `template/` directory name.
- [x] Step 3: Fix wiring issues (if any) so that all routes render: `/`, `/about`, `/projects`, `/contact`, `/login`.
- [x] Step 4: Ensure `/static/css/style.css` loads correctly.

- [x] Optional: Ensure `template/index.html` is either wired or clearly unused (no runtime errors).
- [x] Step 5: Add/Update `README.md` with run instructions.
- [x] Step 6: Re-run app and verify in browser.



